# 🚀 Руководство по установке и запуску проекта

## Первоначальная установка (после клонирования)

### 1. Установите зависимости
```bash
npm install
```

### 2. Настройте базу данных

У вас есть два варианта:

#### Вариант А: Использовать существующие миграции (рекомендуется)
```bash
npx prisma migrate deploy
```
Эта команда применит все существующие миграции из папки `prisma/migrations` и создаст все таблицы в базе данных.

#### Вариант Б: Синхронизировать схему напрямую (быстрый способ для разработки)
```bash
npx prisma db push
```
Эта команда синхронизирует схему Prisma с базой данных без создания файлов миграций.

### 3. Сгенерируйте Prisma Client
```bash
npx prisma generate
```
Эта команда создаст TypeScript типы для работы с базой данных.

### 4. Заполните базу тестовыми данными (опционально)
```bash
npm run db:seed-large
```
Создаст 100 пользователей, чаты, объявления и другие тестовые данные.

### 5. Запустите приложение
```bash
npm run dev
```
Приложение будет доступно по адресу: http://localhost:3000

---

## Разница между командами Prisma

### `npx prisma generate`
- **Что делает:** Генерирует Prisma Client (TypeScript код) на основе схемы
- **Когда использовать:** После изменения `schema.prisma` или после `npm install`
- **НЕ создает:** Таблицы в базе данных

### `npx prisma migrate dev`
- **Что делает:** Создает новую миграцию и применяет её к базе данных
- **Когда использовать:** При разработке, когда вы изменили схему
- **Создает:** Файл миграции + применяет изменения к БД + запускает `generate`

### `npx prisma migrate deploy`
- **Что делает:** Применяет существующие миграции к базе данных
- **Когда использовать:** При первой установке проекта или на продакшене
- **Создает:** Таблицы в БД на основе существующих миграций

### `npx prisma db push`
- **Что делает:** Синхронизирует схему с БД напрямую (без миграций)
- **Когда использовать:** Для быстрого прототипирования
- **Создает:** Таблицы в БД, но НЕ создает файлы миграций

---

## Типичные проблемы и решения

### Ошибка: "The table `main.User` does not exist"
**Причина:** База данных не создана или миграции не применены

**Решение:**
```bash
npx prisma migrate deploy
# или
npx prisma db push
```

### Ошибка: "Cannot find module '@prisma/client'"
**Причина:** Prisma Client не сгенерирован

**Решение:**
```bash
npx prisma generate
```

### Ошибка при входе: "Invalid email or password"
**Причина:** Пользователь не существует в базе данных

**Решение:**
```bash
npm run db:seed-large
```
Затем используйте: `test@example.com` / `password123`

---

## Полная последовательность команд для нового проекта

```bash
# 1. Установить зависимости
npm install

# 2. Применить миграции
npx prisma migrate deploy

# 3. Сгенерировать Prisma Client
npx prisma generate

# 4. Заполнить тестовыми данными
npm run db:seed-large

# 5. Запустить приложение
npm run dev
```

---

## Тестовый аккаунт

После выполнения `npm run db:seed-large`:

- **Email:** test@example.com
- **Пароль:** password123

---

## Дополнительные команды

### Просмотр базы данных
```bash
npx prisma studio
```
Откроет веб-интерфейс для просмотра и редактирования данных.

### Сброс базы данных
```bash
npx prisma migrate reset
```
Удалит все данные и применит миграции заново.

### Создание новой миграции
```bash
npx prisma migrate dev --name your_migration_name
```
Используйте после изменения `schema.prisma`.

---

## Структура проекта

```
nextjs-app/
├── prisma/
│   ├── schema.prisma          # Схема базы данных
│   ├── seed.ts                # Базовые тестовые данные
│   ├── seed-large.ts          # Расширенные тестовые данные
│   └── migrations/            # Файлы миграций
├── src/
│   ├── app/                   # Next.js страницы и API
│   ├── components/            # React компоненты
│   └── lib/                   # Утилиты и конфигурация
└── package.json